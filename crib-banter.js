/* ============================================================================
   THE ULTIMATE SHIFT ENGINE — crib-banter.js  (v3, retired)
   ============================================================================

   This file used to add the banter, the distractions and the tempo curve on
   top of the old game. All three now live inside crib.js, which owns the game
   outright.

   It is deliberately left as a do-nothing file rather than deleted, because
   index.html still has a <script src="crib-banter.js"> line in it. Removing
   that line means editing index.html; leaving this stub in place costs
   nothing and keeps the repo tidy.

   If you ever do tidy index.html, delete that script line and this file
   together. Until then, leave it be.
   ========================================================================= */

/* intentionally empty */
